using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Box_Tape : MonoBehaviour
{
    /*
    public ShllefAnchore s_sa;
    public Transform t_Anchore;
    public GameObject[] g_Anchores;
    public GameObject g_GhostTapeBox;
    private void OnTriggerEnter(Collider other) 
    {
        if(other.tag == "AnchoreBox")
        {
            g_GhostTapeBox.SetActive(false);
            s_sa.b_DeactiveObj = true;
            s_sa.ResetLeft();
            s_sa.ResetRight();
            transform.parent = t_Anchore;
            transform.localPosition = Vector3.zero;
            transform.localRotation = Quaternion.identity;
            
        }

    }
*/
}
